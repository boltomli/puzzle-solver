#!/bin/bash
# Init script for Puzzle Solver QoL mission
# Ensure dependencies are synced
cd "C:\exp\puzzle-solver"
echo "Environment ready: Python $(.venv/Scripts/python --version), uv sync already done."
